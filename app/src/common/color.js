import { createTheme } from "@mui/material/styles";

const concept = createTheme({
  palette: {
    primary: {
      main: "#51DACF",
      contrastText: "#FFF",
    },
    secondary: {
      main: "#41AAA8",
    },
  },
});

export default concept;
